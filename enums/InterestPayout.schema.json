{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "Enums.InterestPayout.schema.json",
  "title": "InterestPayout Type",
  "description": "Enumeration of type of interest payout (e.g. deferred or cash payment)",
  "type": "string",
  "enum": ["DEFERRED", "CASH"]
}
