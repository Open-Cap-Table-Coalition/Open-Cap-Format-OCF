{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "Types.StockClassConversionRights.schema.json",
  "title": "StockClassConversionRights",
  "description": "Type representation of a stock class or series conversion rights into another stock class or series as an object.",
  "type": "object",
  "properties": {
    "ratio": {
      "description": "What number of target StockClass shares do one share of this StockClass convert into",
      "$ref": "Types.Ratio.schema.json"
    },
    "converts_to_future_round": {
      "description": "Is this StockClass potentially convertible into a future, as-yet undetermined StockClass (e.g. Founder Preferred)",
      "type": "boolean"
    },
    "converts_to_stock_class_id": {
      "description": "What is the id of the StockClass this StockClass can convert into?",
      "type": "string"
    },
    "rounding_type": {
      "description": "How should fractional shares be rounded?",
      "$ref": "Enums.Rounding.schema.json"
    }
  },
  "oneOf": [
    {"required": ["converts_to_future_round"]},
    {"required": ["converts_to_stock_class_id"]}
  ],
  "required": [
    "ratio",
    "rounding_type"
  ]
}
