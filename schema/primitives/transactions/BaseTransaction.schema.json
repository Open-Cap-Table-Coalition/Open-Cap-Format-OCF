{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "Primitives.Transactions.BaseTransaction.schema.json",
  "title": "Primitive - Security Transaction",
  "description": "Abstract transaction object to be extended by all other transaction objects.",
  "type": "object",
  "allOf": [{ "$ref": "Primitives.BaseObject.schema.json" }],
  "properties": {
    "security_id": {
      "description": "Identifier for the security which the transaction applies to",
      "type": "string"
    },
    "date": {
      "description": "Date on which the transaction occurred",
      "$ref": "Types.DateString.schema.json"
    }
  },
  "required": ["security_id", "date"]
}
