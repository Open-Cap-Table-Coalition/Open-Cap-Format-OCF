{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://opencaptablecoalition.com/schema/files/ocf_manifest_file",
  "title": "OCF Manifest File",
  "description": "Top-level schema describing the OCF Manifest, which holds issuer information and references core ocf files containing transactions, stakeholders, stock classes, etc.",
  "type": "object",
  "allOf": [{ "$ref": "/schema/primitives/base_file" }],
  "properties": {
    "ocf_version": {
      "description": "OCF Version Identifier",
      "$ref": "/schema/enums/ocf_version_type"
    },
    "file_type": {
      "const": "OCF_MANIFEST_FILE"
    },
    "issuer": {
      "description": "Issuer for the cap table",
      "$ref": "/schema/objects/issuer"
    },
    "comments": {
      "title": "Cap Table - Comment Array",
      "description": "Unstructured text comments related to and stored for the cap table",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "stock_plans_files": {
      "title": "Cap Table - Stock Plans File",
      "description": "File containing list of issued stock plans, indexed from first (0) to last (n), for the cap table (See separate /schema/files/stock_plans_file schema to validate loaded files)",
      "type": "array",
      "items": {
        "$ref": "/schema/types/file"
      }
    },
    "stock_legend_templates_files": {
      "title": "Cap Table - Stock Legend Templates File",
      "description": "File containing stock legend templates for the cap table (See separate /schema/files/stock_legend_templates_file schema to validate loaded files)",
      "type": "array",
      "items": {
        "$ref": "/schema/types/file"
      }
    },
    "stock_classes_files": {
      "title": "Cap Table - Stock Classes File",
      "description": "File containing list of stock classes for the cap table (See separate /schema/files/stock_classes_file schema to validate loaded files)",
      "type": "array",
      "items": {
        "$ref": "/schema/types/file"
      }
    },
    "vesting_schedules_files": {
      "title": "Cap Table - Vesting Schedules File",
      "description": "File containing list of vesting schedules for the cap table (See separate /schema/files/vesting_schedules_file schema to validate loaded files)",
      "type": "array",
      "items": {
        "$ref": "/schema/types/file"
      }
    },
    "valuations_files": {
      "title": "Cap Table - Valuation Array",
      "description": "File containing list of valuations for the cap table (See separate /schema/files/valuations_file schema to validate loaded files)",
      "type": "array",
      "items": {
        "$ref": "/schema/types/file"
      }
    },
    "transactions_files": {
      "title": "Cap Table - Transaction File(s) Array",
      "description": "List of files referencing transactions, indexed from first (0) to last (n), for the cap table (See separate /schema/files/transactions_file schema to validate loaded files)",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "/schema/types/file"
      }
    },
    "stakeholders_files": {
      "title": "Cap Table - Stakeholder File(s) Array",
      "description": "List of files referencing stakeholders, indexed from first (0) to last (n), for the cap table  (See separate /schema/files/stakeholders_file schema to validate loaded files)",
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "/schema/types/file"
      }
    }
  },
  "required": [
    "ocf_version",
    "file_type",
    "issuer",
    "comments",
    "stock_plans_files",
    "stock_legend_templates_files",
    "stock_classes_files",
    "vesting_schedules_files",
    "valuations_files",
    "transactions_files",
    "stakeholders_files"
  ],
  "additionalProperties": false
}
